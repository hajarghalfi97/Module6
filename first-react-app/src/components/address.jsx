function address({street, state = 'NSW', zipcode = '12345',
    children}) {
    // destructuring the props.children property as well
    
    return (
    <div className="address componentbox">
    <strong>{name}</strong> is in {state}, {country}

    </div>
    )}